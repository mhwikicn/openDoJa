package com.nttdocomo.system;

/**
 * Defines constants related to the phone book.
 */
public interface PhoneBookConstants {
    /** Indicates the family-name part (=0). */
    int FAMILY_NAME = 0;

    /** Indicates the given-name part (=1). */
    int GIVEN_NAME = 1;
}
